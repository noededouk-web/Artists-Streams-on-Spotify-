app = 'stream-analytics-front'
primary_region = 'ams'

[build]
  dockerfile = 'dockerfile'

[env]
  API_URL = 'https://artists-streams-on-spotify-youtube.fly.dev'

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0

[[vm]]
  size = 'shared-cpu-1x'
  memory = '256mb'
